@model SZZP2._2.Models.Office

@{
    ViewData["Title"] = "Details";
}

<h2>Szczegóły Biura</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt style="text-align:left">
            @Html.DisplayNameFor(model => model.NameOffice)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.NameOffice)
        </dd>
        <dt style="text-align:left">
            @Html.DisplayNameFor(model => model.SymbolOffice)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.SymbolOffice)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-success" asp-route-id="@Model.IDOffice">Edytuj</a> |
    <a asp-action="Index" class="btn btn-primary">Powrót</a>
</div>

<hr />
<h2 style="text-align:center">Wydziały</h2>
<hr />
@*<p>
    <a asp-action="CreateDepartment">Create New</a>
</p>*@
<div>
    <table class="table">
        <tr>
            <th>Nazwa wydziału</th>
            <th>Symbol</th>
        </tr>

        @foreach (var item in Model.Departments)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NameDepartment)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SymbolDeprtament)
                </td>
                @*<td>
                    <a asp-action="EditDepartment" asp-route-id="@item.IDDepartment">Edit</a> |
                    <a asp-action="DeleteDepartment" asp-route-id="@item.IDDepartment">Delete</a>
                </td>*@
            </tr>
        }
    </table>
</div>

